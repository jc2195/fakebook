<%= form_with model: @friend_request, url: friend_requests_path, method: 'post' do |f| %>
  <%= f.hidden_field 'sender_id', :value => current_user[:id] %>
  <%= f.hidden_field 'receiver_id', :value => user.id %>
  <%= f.submit "Send Friend Request" %>
<% end %>